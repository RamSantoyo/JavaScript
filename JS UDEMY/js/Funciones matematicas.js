// Objeto Math

let resultado;

resultado = Math.PI;
resultado = Math.round(4.5);//redondea al numero mas cercano
resultado = Math.ceil(2.1); //Ceil siempre redondea hacia arriba
resultado = Math.floor(2.9);//Floor siempre redondea hacia abajo
resultado = Math.sqrt(144);//Raiz cuadrada
resultado = Math.abs(-200);//cambia el valor a positivo
resultado = Math.min( 3, 5, 1, 8 , 2, 10 );//Buscfa el valor minimo
resultado = Math.max( 3, 5, 1, 8 , 2, 10 );//Busca el valor maximo
resultado = Math.random();//Un  numero aletorio pero en decimal
resultado = Math.ceil( Math.random() * 5);//Un  numero aletorio entero con un rango


console.log(resultado);